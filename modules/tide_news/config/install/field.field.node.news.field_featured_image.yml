langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_featured_image
    - media.type.image
    - node.type.news
id: node.news.field_featured_image
field_name: field_featured_image
entity_type: node
bundle: news
label: 'Featured Image'
description: '<p>The feature image displays by default below the date on published news pages. You have the option to turn off the feature image. The feature image displays in promotion or navigation cards linking to this page when Card display style is set as Thumbnail or Profile. JPEG is the preferred format.</p><p>See the SDP guide on <a href="https://digital-vic.atlassian.net/wiki/spaces/FPSDP/pages/2304212993/Image+ratios+sizes+and+component+use">image ratios, sizes and component use</a>.</p>'
required: false
translatable: true
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:media'
  handler_settings:
    target_bundles:
      image: image
    sort:
      field: _none
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
