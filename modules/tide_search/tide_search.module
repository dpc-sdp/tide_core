<?php

/**
 * @file
 * Tide Search module functionality.
 */

use Drupal\tide_search\Hook\TideSearchHooks;
use Drupal\Core\Entity\EntityInterface;
use Drupal\Core\Form\FormStateInterface;
use Drupal\media\MediaInterface;
use Drupal\search_api\Entity\Index;
use Drupal\search_api\IndexInterface;

/**
 * Implements hook_modules_installed().
 */
#[\Drupal\Core\Hook\Attribute\LegacyHook]
function tide_search_modules_installed(array $modules, bool $is_syncing): void {
  \Drupal::service(TideSearchHooks::class)->modulesInstalled($modules, $is_syncing);
}

/**
 * Implements hook_form_BASE_FORM_ID_alter().
 */
#[\Drupal\Core\Hook\Attribute\LegacyHook]
function tide_search_form_node_form_alter(&$form, FormStateInterface $form_state, $form_id) {
  return \Drupal::service(TideSearchHooks::class)->formNodeFormAlter($form, $form_state, $form_id);
}

/**
 * Implements hook_paragraphs_type_widget_alter().
 */
#[\Drupal\Core\Hook\Attribute\LegacyHook]
function tide_search_field_widget_single_element_paragraphs_form_alter(array &$element, FormStateInterface $form_state, array $context) {
  return \Drupal::service(TideSearchHooks::class)->fieldWidgetSingleElementParagraphsFormAlter($element, $form_state, $context);
}

/**
 * Implements hook_search_api_index_items_alter().
 */
#[\Drupal\Core\Hook\Attribute\LegacyHook]
function tide_search_search_api_index_items_alter(IndexInterface $index, array &$items) {
  return \Drupal::service(TideSearchHooks::class)->searchApiIndexItemsAlter($index, $items);
}

/**
 * Converts timestamp to RFC-3339 format.
 *
 * @param int $ts
 *   Timestamp.
 *
 * @return string
 *   Formatted date.
 */
function _tide_search_get_formatted_date($ts) {
  if (!is_numeric($ts)) {
    return $ts;
  }
  $config = \Drupal::config('system.date');
  $timezone = new DateTimeZone($config->get('timezone.default'));
  $date = new \Datetime();
  $date->setTimezone($timezone);
  $date = $date->setTimestamp($ts);

  return $date->format('Y-m-d\TH:i:sP');
}

/**
 * Implements hook_admin_audit_trail_handlers().
 */
#[\Drupal\Core\Hook\Attribute\LegacyHook]
function tide_search_admin_audit_trail_handlers() {
  return \Drupal::service(TideSearchHooks::class)->adminAuditTrailHandlers();
}

/**
 * Implements hook_form_BASE_FORM_ID_alter().
 */
function _tide_search_form_node_form_process(array $form, FormStateInterface $form_state, array $complete_form = []) {
  /** @var \Drupal\node\NodeInterface $node */
  $node = $form_state->getFormObject()->getEntity();

  // Add pseudo radio buttons for Header style.
  $form['_header_style'] = [
    '#type' => 'container',
    '#group' => 'group_customised_header',
    '#weight' => -100,
  ];
  $form['_header_style']['_header_style_options'] = [
    '#title' => t('Header style'),
    '#description' => t('The header can be customised to incorporate a custom top corner graphic.'),
    '#type' => 'radios',
    '#required' => TRUE,
    '#options' => [
      'default' => t('Default appearance'),
      'corner' => t('Custom top corner graphic'),
    ],
  ];
  $header_style = \Drupal::state()->get($node->uuid() . '-header_style');
  $form['_header_style']['_header_style_options']['#default_value'] = $header_style ?: 'default';

  // Control of the graphical image field based on the selected header style.
  $form['field_graphical_image']['#states']['visible'] = [
    ':input[name="_header_style_options"]' => ['value' => 'corner'],
  ];

  return $form;
}

/**
 * Implements hook_config_schema_info_alter().
 */
#[\Drupal\Core\Hook\Attribute\LegacyHook]
function tide_search_config_schema_info_alter(&$schema) {
  return \Drupal::service(TideSearchHooks::class)->configSchemaInfoAlter($schema);
}

/**
 * Implements after build for tide search listings.
 */
function _tide_search_form_node_form_after_build(array $form, FormStateInterface $form_state) {
  $current_user = \Drupal::currentUser();
  $admin_roles = ['administrator', 'site_admin'];

  // Self service search listing.
  $non_admin = !array_intersect($admin_roles, $current_user->getRoles());
  if ($non_admin) {
    $groups = [
      'group_listing_configuration',
      'group_global_filters',
      'group_user_filters',
      'group_results',
    ];
    $restricted_items = [
      'field_custom_sort_configuration',
      'field_listing_query_config',
      'field_search_submit_label',
      'field_listing_results_config',
      'field_search_configuration',
      'field_landing_page_c_secondary',
      'field_layout_component',
      'field_results_component',
      'group_custom_header',
    ];
    foreach ($restricted_items as $field_name) {
      $form[$field_name]['#access'] = FALSE;
    }
    foreach ($groups as $group) {
      if (isset($form[$group])) {
        $form[$group]['#open'] = TRUE;
      }
    }
    // Hide global & user filter's custom filter paragraph.
    $targets = [
      'field_listing_global_filters' => 'add_more_button_listing_custom_filter',
      'field_listing_user_filters'   => 'add_more_button_listing_user_custom_filter',
    ];
    foreach ($targets as $field => $button) {
      if (isset($form[$field]['widget']['add_more'][$button])) {
        unset($form[$field]['widget']['add_more'][$button]);
      }
    }
    // Mandatory site field.
    if (!empty($form['field_listing_global_filters']['widget'])) {
      foreach ($form['field_listing_global_filters']['widget'] as $delta => &$widget) {
        if (!is_numeric($delta)) {
          continue;
        }

        // Target the listing_site paragraph.
        if (isset($widget['#paragraph_type']) && $widget['#paragraph_type'] === 'listing_site') {
          if (isset($widget['subform']['field_listing_site_site'])) {
            $widget['subform']['field_listing_site_site']['widget']['#required'] = TRUE;
          }
        }
      }
    }
    // Collapsed by default.
    $wrappable_fields = [
      'field_above_results_content' => 'Above results content',
      'field_below_results_content' => 'Below results content',
    ];

    foreach ($wrappable_fields as $field_name => $title) {
      if (isset($form[$field_name])) {
        $form["{$field_name}_wrapper"] = [
          '#type' => 'details',
          '#title' => $title,
          '#open' => FALSE,
          '#weight' => $form[$field_name]['#weight'] ?? 0,
        ];

        // Move field inside wrapper.
        $form["{$field_name}_wrapper"][$field_name] = $form[$field_name];

        // Remove the original.
        unset($form[$field_name]);
      }
    }
    $form['#attached']['library'][] = 'tide_search/search_listing_form';
  }
  // Non admin field only.
  $form['field_listing_result_type']['#access'] = !$non_admin ? FALSE : TRUE;
  $form['field_sort_by_config_non_admin']['#access'] = !$non_admin ? FALSE : TRUE;

  return $form;
}

/**
 * Implements hook_entity_presave().
 */
#[\Drupal\Core\Hook\Attribute\LegacyHook]
function tide_search_entity_presave(EntityInterface $entity) {
  return \Drupal::service(TideSearchHooks::class)->entityPresave($entity);
}

/**
 * Removes empty paragraph items from an entity reference field.
 *
 * @param \Drupal\Core\Entity\EntityInterface $entity
 *   The entity being cleaned.
 * @param string $field_name
 *   The entity reference field machine name.
 * @param string $bundle
 *   The paragraph bundle to check.
 * @param string $field_to_check
 *   The field inside the paragraph to test for emptiness.
 */
function _tide_search_clean_empty_paragraphs(EntityInterface $entity, string $field_name, string $bundle, string $field_to_check): void {
  if (!$entity->hasField($field_name)) {
    return;
  }

  $items = $entity->get($field_name);

  foreach ($items as $delta => $item) {
    $paragraph = $item->entity;
    if ($paragraph && $paragraph->bundle() === $bundle) {
      if ($paragraph->get($field_to_check)->isEmpty()) {
        $items->removeItem($delta);
      }
    }
  }
}

/**
 * Helper function to prefill form field values based on query preset.
 *
 * @param array $form
 *   The node form array (passed by reference).
 */
function _tide_search_apply_preset_values(array &$form, FormStateInterface $form_state) {
  $preset = \Drupal::request()->query->get('preset');
  if (!$preset) {
    return;
  }

  // Prefill sort config.
  $valid_presets = ['news', 'publications', 'grants', 'events', 'landing_page'];
  if ($preset && in_array($preset, $valid_presets, TRUE)) {
    $form['field_sort_by_config_non_admin']['widget']['#default_value'][0] = $preset;
  }

  // Query configuration mapping.
  $query_fields = [
    'field_landing_page_nav_title',
    'field_landing_page_summary',
    'field_landing_page_intro_text',
    'field_news_intro_text',
    'field_paragraph_body',
    'field_overview_title',
    'summary_processed',
    'title',
    'body',
  ];

  // Apply query configuration.
  if ($preset && isset($form['field_listing_query_config'])) {
    $query_config_value = [
      'multi_match' => [
        'query' => '{{query}}',
        'fields' => $query_fields,
      ],
    ];
    // Encode as JSON and set as default value.
    $form['field_listing_query_config']['widget'][0]['value']['#default_value'] = json_encode($query_config_value);
  }

  // Page title mapping.
  $title_map = [
    'news'         => 'Create news search listing',
    'publications' => 'Create publications search listing',
    'grants'       => 'Create grants search listing',
    'events'       => 'Create events search listing',
    'landing_page' => 'Create landing page search listing',
  ];

  // Apply page title.
  if (isset($title_map[$preset])) {
    $form['#title'] = $title_map[$preset];
  }

  // Mapping preset → content type machine name.
  $content_type_map = [
    'news'         => 'news',
    'publications' => 'publication',
    'grants'       => 'grant',
    'events'       => 'event',
    'landing_page' => 'landing_page',
  ];

  if (!empty($preset) && isset($content_type_map[$preset])) {
    // Make sure the paragraph widget exists before trying to set defaults.
    if (isset($form['field_listing_global_filters']['widget'][0]['subform']['field_listing_global_contenttype']['widget'])) {
      $form['field_listing_global_filters']['widget'][0]['subform']['field_listing_global_contenttype']['widget']['#default_value'] = [$content_type_map[$preset]];
    }
  }

  // Map presets to field_listing_result_type values.
  $result_type_map = [
    'events'       => 'card',
    'grants'       => 'grants',
    'landing_page' => 'list',
    'news'         => 'card',
    'publications' => 'card',
  ];

  // Set the value for result type.
  if (isset($result_type_map[$preset]) && isset($form['field_listing_result_type'])) {
    $form['field_listing_result_type']['widget']['#default_value'][0] = $result_type_map[$preset];
  }

  // Set results per page: based on preset.
  if (isset($form['field_listing_results_per_page'])) {
    $per_page = in_array($preset, ['grants', 'landing_page'], TRUE) ? 10 : 12;
    $form['field_listing_results_per_page']['widget'][0]['value']['#default_value'] = $per_page;
  }

  // Prefill Search Configuration.
  if (isset($preset) && $preset == 'grants') {
    $form['field_search_configuration']['widget'][0]['value']['#default_value'] = '{"customQueryConfig": {"function": "grantsQueryFn"}}';
  }
}

/**
 * Implements hook_theme().
 */
#[\Drupal\Core\Hook\Attribute\LegacyHook]
function tide_search_theme($existing, $type, $theme, $path) {
  return \Drupal::service(TideSearchHooks::class)->theme($existing, $type, $theme, $path);
}

/**
 * Implements hook_theme_suggestions_HOOK().
 */
#[\Drupal\Core\Hook\Attribute\LegacyHook]
function tide_search_theme_suggestions_node_add_list(array $variables) {
  return \Drupal::service(TideSearchHooks::class)->themeSuggestionsNodeAddList($variables);
}

/**
 * Implements hook_entity_insert().
 */
#[\Drupal\Core\Hook\Attribute\LegacyHook]
function tide_search_media_insert(MediaInterface $entity) {
  return \Drupal::service(TideSearchHooks::class)->mediaInsert($entity);
}

/**
 * Implements hook_entity_update().
 */
#[\Drupal\Core\Hook\Attribute\LegacyHook]
function tide_search_media_update(MediaInterface $entity) {
  return \Drupal::service(TideSearchHooks::class)->mediaUpdate($entity);
}

/**
 * Implements hook_entity_delete().
 */
#[\Drupal\Core\Hook\Attribute\LegacyHook]
function tide_search_media_delete(MediaInterface $entity) {
  return \Drupal::service(TideSearchHooks::class)->mediaDelete($entity);
}

/**
 * Helper function to notify Search API indexes about Media changes.
 */
function _tide_search_search_api_sync(MediaInterface $entity, $action) {
  $datasource_id = 'filtered_media';
  // Load all search indexes.
  $indexes = Index::loadMultiple();

  foreach ($indexes as $index) {
    if (!$index->isValidDatasource($datasource_id)) {
      continue;
    }

    // Get the datasource instance and its specific config.
    $datasource = $index->getDatasource($datasource_id);
    $config = $datasource->getConfiguration();

    $target_bundles = $config['bundles'] ?? [];
    $indexing_field = $config['indexing_field'] ?? '';

    // Only proceed if this Media entity belongs.
    if (!in_array($entity->bundle(), $target_bundles)) {
      continue;
    }

    $item_id = $entity->id();

    // Determine if the item meets the indexing criteria.
    $is_published = $entity->isPublished();
    $field_exists = !empty($indexing_field) && $entity->hasField($indexing_field);
    $is_field_checked = $field_exists && (bool) $entity->get($indexing_field)->value;

    // Logic for tracking changes.
    // If deleted OR it no longer meets criteria.
    // Remove from index tracker.
    if ($action === 'delete' || !$is_published || !$is_field_checked) {
      $index->trackItemsDeleted($datasource_id, [$item_id]);
    }
    // Otherwise, mark as inserted or updated.
    else {
      $index->trackItemsInserted($datasource_id, [$item_id]);
    }
  }
}
